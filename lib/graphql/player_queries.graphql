query play($videoId:String!,$url:String!,$filePath:String){
  play(videoId:$videoId,url:$url,filePath:$filePath)
}
query pause{
  pause
}
query resume{
  resume
}
query seek($seconds:Int!){
  seek(seconds:$seconds)
}